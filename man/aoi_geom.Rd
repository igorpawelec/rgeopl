% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aoi.R
\name{aoi_geom}
\alias{aoi_geom}
\alias{aoi_bbox}
\title{Geometry and bounding box of an area of interest}
\usage{
aoi_geom(aoi, crs = CRS_PL1992)

aoi_bbox(aoi, crs = CRS_PL1992, by_feature = FALSE)
}
\arguments{
\item{aoi}{An `rgeopl_aoi` object, or anything [as_aoi()] accepts.}

\item{crs}{Target CRS. Defaults to EPSG:2180, the CRS the GUGiK services
use natively.}

\item{by_feature}{When `TRUE`, return one bounding box per feature instead
of a single box around all of them. Useful for scattered areas, where one
overall box would cover far more ground than was asked for.}
}
\value{
`aoi_geom()` an `sfc`; `aoi_bbox()` a named numeric vector
  (`xmin`, `ymin`, `xmax`, `ymax`), or a list of them when `by_feature`.
}
\description{
Geometry and bounding box of an area of interest
}
\examples{
aoi <- as_aoi(c(16.93, 52.41), buffer = 250)
aoi_bbox(aoi)
aoi_bbox(aoi, crs = 4326)

}
