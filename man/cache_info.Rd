% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{cache_info}
\alias{cache_info}
\alias{cache_clear}
\title{Inspect and prune the download cache}
\usage{
cache_info(group = NULL)

cache_clear(
  group = NULL,
  older_than = NULL,
  meta = FALSE,
  confirm = interactive()
)
}
\arguments{
\item{group}{Restrict to one group (for example `"dem"`, `"ortho"`,
`"pointcloud"`, `"bdl"`). `NULL` means all groups.}

\item{older_than}{Drop entries downloaded more than this many days ago.
`NULL` means no age limit.}

\item{meta}{Also clear the cached index responses.}

\item{confirm}{Ask before deleting. Set to `FALSE` in scripts.}
}
\value{
`cache_info()` a data frame, one row per cached file, with a
  summary printed. `cache_clear()` returns the number of files removed,
  invisibly.
}
\description{
Inspect and prune the download cache
}
\examples{
\dontrun{
cache_info()
cache_clear(group = "ortho", older_than = 90, confirm = FALSE)
}

}
